// Màu chủ đạo — chỉ sửa trong src/index.css (@theme) là đủ
// PRIMARY_COLOR dùng CSS variable nên tự động đồng bộ
export const PRIMARY_COLOR = 'var(--color-primary)';
export const GOLD_COLOR = 'var(--color-gold)';
